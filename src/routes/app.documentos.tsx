import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { EmpresaCard, PageState } from "@/components/prazocontabil/ui";
import { WorkspaceData, emptyWorkspaceData, fetchWorkspaceData } from "@/lib/prazocontabil-data";
export const Route=createFileRoute("/app/documentos")({head:()=>({meta:[{title:"Documentos — PrazoContábil"},{name:"description",content:"Documentos organizados por empresa com situação de vencimento e arquivo."}]}),component:DocumentosPage});
function DocumentosPage(){const[data,setData]=useState<WorkspaceData>(emptyWorkspaceData());const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{fetchWorkspaceData().then(setData).catch(()=>setError("Não foi possível carregar os documentos.")).finally(()=>setLoading(false))},[]);if(loading||error)return <PageState loading={loading} error={error}/>;return <div className="space-y-5"><section className="surface-card rounded-lg border p-5"><h2 className="text-xl font-semibold">Documentos por empresa</h2><p className="mt-1 text-sm text-muted-foreground">Cada card mostra válidos, vencendo, vencidos, sem arquivo e próximo vencimento.</p></section><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.empresas.map(e=><EmpresaCard key={e.id} empresa={e} documentos={data.documentos}/>)}</div>{!data.empresas.length&&<PageState emptyText="Nenhum registro encontrado para os filtros selecionados."/>}</div>}
